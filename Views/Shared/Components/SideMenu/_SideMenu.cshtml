@model IEnumerable<Film>

<div class="col-sm-2 sidenav">
    @foreach (var film in Model)
    {
        <p><a href="@Url.Action("Details","Films", new{Id_film=film.Id_Film})">@film.Title</a></p>
    }
</div>
